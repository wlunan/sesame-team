# 🚀 快速开始指南

## 项目已创建完成！

你的芝麻分组队匹配网站已经搭建完毕，包含以下功能：

✅ **用户系统**：注册、登录、登出  
✅ **分数提交**：提交芝麻分和口令  
✅ **智能匹配**：三数之和等于2026的自动匹配  
✅ **搜索功能**：搜索特定分数  
✅ **匹配记录**：查看我的匹配历史  
✅ **举报机制**：标记失效口令  
✅ **响应式设计**：支持移动端和桌面端  

---

## 📋 下一步操作

### 1. 配置 Supabase

#### 步骤 1：创建 Supabase 项目
1. 访问 https://supabase.com 并注册账号
2. 点击 "New Project" 创建新项目
3. 记录项目的 URL 和 API Key

#### 步骤 2：初始化数据库
1. 在 Supabase Dashboard，进入 **SQL Editor**
2. 复制 `supabase/schema.sql` 的全部内容
3. 粘贴并点击 **RUN** 执行
4. 同样执行 `supabase/functions.sql`

#### 步骤 3：配置环境变量
编辑 `.env` 文件，填入你的配置：

```env
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key_here
```

### 2. 启动开发服务器

```bash
npm run dev
```

访问 http://localhost:5173 查看效果！

### 3. 部署 Edge Function（可选但推荐）

```bash
# 安装 Supabase CLI
npm install -g supabase

# 登录
supabase login

# 关联项目
supabase link --project-ref your-project-ref

# 部署匹配函数
supabase functions deploy match-scores
```

### 4. 设置定时任务

在 Supabase SQL Editor 中执行：

```sql
CREATE EXTENSION IF NOT EXISTS pg_cron;

SELECT cron.schedule(
  'match-scores-job',
  '* * * * *',  -- 每分钟执行一次
  $$
  SELECT net.http_post(
    url := 'https://your-project.supabase.co/functions/v1/match-scores',
    headers := '{"Content-Type": "application/json", "Authorization": "Bearer your-service-role-key"}'::jsonb,
    body := '{}'::jsonb
  );
  $$
);
```

---

## 📁 项目结构

```
supabase-demo/
├── src/
│   ├── components/          # Vue 组件
│   │   ├── AuthForm.vue        # 登录/注册表单
│   │   ├── ScoreSubmit.vue     # 分数提交
│   │   ├── SearchScore.vue     # 搜索分数
│   │   └── MatchList.vue       # 匹配记录列表
│   ├── views/              # 页面视图
│   │   ├── Home.vue            # 首页
│   │   └── MyMatches.vue       # 我的匹配页
│   ├── stores/             # Pinia 状态管理
│   │   └── auth.js             # 认证状态
│   ├── lib/                # 工具库
│   │   └── supabase.js         # Supabase 客户端
│   ├── router/             # 路由配置
│   │   └── index.js
│   ├── App.vue             # 根组件
│   ├── main.js             # 入口文件
│   └── style.css           # 全局样式
├── supabase/
│   ├── schema.sql          # 数据库表结构
│   ├── functions.sql       # 数据库函数
│   └── functions/          # Edge Functions
│       └── match-scores/   # 匹配算法
├── package.json
├── vite.config.js
├── tailwind.config.js
└── README.md
```

---

## 🎯 核心功能说明

### 1. 数据库表结构
- **scores** - 存储用户提交的分数和口令
- **matches** - 存储匹配记录
- **reports** - 存储举报记录

### 2. 匹配算法
使用三数之和算法，找出所有总和为2026的组合：
- 优先匹配高分（800+）
- 支持一个分数多次匹配
- 自动创建匹配记录

### 3. 认证系统
- 使用 Supabase Auth
- Email/Password 登录
- Row Level Security (RLS) 保护数据

---

## 🔧 常用命令

```bash
# 开发
npm run dev          # 启动开发服务器
npm run build        # 构建生产版本
npm run preview      # 预览生产版本

# Supabase
supabase functions deploy match-scores   # 部署函数
supabase db dump -f backup.sql          # 备份数据库
```

---

## 📚 详细文档

- [完整部署指南](./DEPLOYMENT.md)
- [Supabase 文档](https://supabase.com/docs)
- [Vue 3 文档](https://vuejs.org)
- [TailwindCSS 文档](https://tailwindcss.com)

---

## 🐛 故障排查

### 登录失败
- 检查 `.env` 配置是否正确
- 确认数据库表已创建
- 查看浏览器控制台错误信息

### 匹配不工作
- 确认 Edge Function 已部署
- 检查定时任务是否设置
- 查看 Supabase Function Logs

### 样式异常
- 清除浏览器缓存
- 重启开发服务器
- 检查 TailwindCSS 配置

---

## 🎨 自定义修改

### 修改目标总和
在 `supabase/functions/match-scores/index.ts` 中：
```typescript
const TARGET_SUM = 2026  // 改为其他值
```

同时更新前端显示文案。

### 添加更多匹配规则
编辑匹配算法，添加自定义逻辑：
- VIP 用户优先
- 相同地区优先
- 积分奖励机制

### 自定义界面风格
修改 `src/style.css` 和 `tailwind.config.js`：
```javascript
theme: {
  extend: {
    colors: {
      primary: '#your-color',
    }
  }
}
```

---

## 💡 功能扩展建议

1. **实时通知**：使用 Supabase Realtime 订阅
2. **社交分享**：添加分享到社交媒体功能
3. **积分系统**：成功匹配获得积分
4. **排行榜**：显示匹配次数最多的用户
5. **数据统计**：展示平台统计数据（总用户、总匹配等）

---

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

---

## 📄 许可

MIT License

---

**祝开发顺利！如有问题，请查看详细文档或提交 Issue。** 🚀
